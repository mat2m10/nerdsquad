<div class = "homepage">
  <div class = "intro">
  <h1>Add dices to your game</h1>
    <h2>Time to toss the dice but first, mention the number of dices needed and how many faces you need your dices to have.</h2>
  <br>
  <br>
  <br>
  <br>
    <div class = "cardwow">
    <%= simple_form_for([@game, @dice]) do |f| %>
        <%= f.input :number_of_dices, collection: 1..8 %>
        <%= f.input :faces %>
        <%= f.input :photo, as: :file %>
        <%= f.button :submit, class: 'btn-flat' %>
    <% end %>
    </div>
  </div>
</div>
