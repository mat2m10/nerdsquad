<h2>Create your shitty-ass BOARD</h2>
<%= simple_form_for([@game, @board], url: game_boards_path, remote: true) do |f| %>
<%= f.input :photo, as: :file %>
    <%= f.button :submit, id: "test"  %>
<% end %>
