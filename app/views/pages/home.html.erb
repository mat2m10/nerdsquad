<div class="homepage">
 <div class="background">
    <div class="container">
      <div class="intro" id="intro">
        <h1>Nerd Squad</h1>
        <h2>Play your favorite games with your friends</h2>
      </div>

      <div class="options">
        <% if @gameroom != nil %>
          <a href='<%= game_gamerooms_path(@game) %>'><button class="big-button">PLAY</button></a>
        <% else %>
          <a href='<%= new_game_path %>'><button class="big-button">PLAY</button></a>         
        <% end %>
        <a href='<%= new_game_path %>'><button class="big-button">CREATE</button></a>
        <a href='<%= games_path %>'><button class="big-button">BROWSE</button></a>
      </div>
      
    </div>
  </div>
</div>
