<div class="table">
</div>

    <h1><%= @game.name %> game preview </h1>
    <h2><%= @game.number_of_players %> players</h2>

    <% if @game.board %>
      <%=cl_image_tag @game.board.photo.key , { class: "board", style: "position: absolute; left: #{@game.board.posX}px; top: #{@game.board.posY}px;"} %>
    <% end %>

      <% unless @game.pieces.empty? %>
        <% @game.pieces.each do |piece| %>
        <div class="movablecontainer">
          <%= cl_image_tag piece.photo.key, { class: "piece", style: "position: absolute; left: #{piece.posX}px; top: #{piece.posY}px;", width: '60px'}%>
        </div>
        <% end %>
      <% end %>

      <% unless @game.tokens.empty? %>
        <% @game.tokens.each do |token| %>
        <div class="movablecontainer">
          <%= cl_image_tag token.photo.key, { class: "token", style: "position: absolute; left: #{token.posX}px; top: #{token.posY}px;", width: '40px'}%>
        </div>
        <% end %>
      <% end %>

      <%# unless @game.dices.empty? %>
        <%#= cl_image_tag("dices.png", :quality=>50, :width=>72, :crop=>"scale") %>
        <%# <ul> %>
          <%# @game.dices.each do |dice| %>
          <%# <li><%#= dice.faces %>   <%#faces</li> %>
          <%# end %>
        <%#</ul>
      <%# end %>

      <% unless @game.card_decks.empty? %>
        <% @game.card_decks.each do |deck| %>
          <div class="movablecontainer">
            <%= deck.name %>
            <%= cl_image_tag deck.cards.first.photo.key, {width: '120px'} %>
          </div>
        <% end %>
      <% end %>

