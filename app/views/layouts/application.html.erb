<!DOCTYPE html>
<html>
  <head>
    <title>Nerdsquad</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application','data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <%= render 'shared/flashes' %>
    <% if controller_name == 'games' && action_name == 'index' || controller_name == 'games' && action_name == 'show'|| controller_name == 'pages' || controller_name == 'users' && action_name == 'show' || controller_name == 'gamerooms' && action_name == 'index' %>
      <%= render 'shared/navbar' %>
    <% end %>
       
    <%= yield %>
  </body>
</html>
